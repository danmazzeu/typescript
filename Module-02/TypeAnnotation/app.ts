// Variables
let fullname: string = 'Daniel Mazzeu';

// Arrays
let animals: string[] = ['Elephant', 'Dog', 'car', 'Panda', 'Giraffe'];

// Objects
let car: {
    name: string;
    year: number;
    price: number;
};

// Functions with Type Annotations
function multiplyNumbers(num1: number, num2: number): number {
    return num1 * num2;
}